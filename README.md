# TalentYug Landing (React + Vite)

Simple React implementation of the provided landing design.

Run locally:

```bash
npm install
npm run dev
```

Open the URL printed by Vite (usually http://localhost:5173).
